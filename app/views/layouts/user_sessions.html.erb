<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="initial-scale=1, width=device-width">
    <title>AutArch Login</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "autarch" %>
    <%= javascript_include_tag "autarch" %>
    <style>
      @font-face {
        font-family: Roboto;
        src: url(/roboto-latin-400-normal.woff);
      }

      .login-body {
        background: linear-gradient(135deg, #1a2a6c 0%, #b21f1f 100%);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 1rem;
      }

      .login-container {
        max-width: 450px;
        width: 100%;
        padding: 2.5rem;
        background: white;
        border-radius: 12px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        transition: transform 0.3s ease;
      }

      .login-container:hover {
        transform: translateY(-5px);
      }

      .login-container h1 {
        color: #2c3e50;
        margin-bottom: 1.5rem;
      }

      .form-control {
        border-radius: 6px;
        border: 1px solid #ced4da;
        padding: 0.75rem 1rem;
        transition: border-color 0.15s ease-in-out, box-shadow 0.15s ease-in-out;
      }

      .form-control:focus {
        border-color: #80bdff;
        box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25);
      }

      .btn-primary {
        background-color: #4285f4;
        border-color: #4285f4;
        padding: 0.75rem 1rem;
        font-weight: 500;
        transition: background-color 0.2s;
      }

      .btn-primary:hover {
        background-color: #3367d6;
        border-color: #2d5aaf;
      }

      .alert {
        border-radius: 6px;
        padding: 0.75rem 1rem;
        margin-bottom: 1.5rem;
      }

      .text-muted {
        color: #6c757d !important;
      }

      .text-muted:hover {
        color: #495057 !important;
        text-decoration: underline;
      }
    </style>
  </head>
  <body class="login-body">
    <div class="login-container">
      <% flash.each do |type, msg| %>
        <div class="alert alert-<%= type == 'alert' ? 'danger' : type %>" role="alert">
          <%= msg %>
        </div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
