<%= form_with(model: grafe) do |form| %>
  <% if grafe.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(grafe.errors.count, "error") %> prohibited this grafe from being saved:</h2>

      <ul>
        <% grafe.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :location, style: "display: block" %>
    <%= form.text_field :location %>
  </div>

  <div>
    <%= form.label :figure_id, style: "display: block" %>
    <%= form.text_field :figure_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
